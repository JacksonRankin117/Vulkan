#version 450

layout(local_size_x = 1) in;

layout(binding = 0) buffer Data {
    uint values[];
} buf;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    buf.values[idx] = idx * 2; // simple test: write double the index
}
